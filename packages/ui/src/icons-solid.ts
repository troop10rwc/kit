/* Re-export of Font Awesome's FREE solid pack (`fa-solid`), the default style for
   the back office. Import from "@troop10rwc/ui/icons/solid" so the kit is the
   single dependency; unused icons tree-shake out (the pack is side-effect free). */
export * from "@fortawesome/free-solid-svg-icons";
